overwrite: true
schema: "https://food-planner-database.onrender.com/v1/graphql"
documents: "src/lib/queries/**"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
